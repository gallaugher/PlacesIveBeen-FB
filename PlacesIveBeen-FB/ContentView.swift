// File: ContentView.swift Project: PlacesIveBeen-FB
// Created by: Prof. John Gallaugher on 11/25/24
// YouTube.com/profgallaugher  -  threads.net/john.gallaugher

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Hello, world!")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
